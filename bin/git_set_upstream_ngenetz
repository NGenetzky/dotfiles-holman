#!/usr/bin/env bash

if [ $# -eq 0 ]; then
    echo 'ERROR:$1 should be branch_name'
    exit 1;
elif [ $# -eq 1 ]; then
    branch_name=$1
    git checkout -b $1 -t origin/ngenetz$1
    # git branch --set-upstream-to origin/ngenetz/$1
fi
